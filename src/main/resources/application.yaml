spring:
  application:
    name: SimplyForge

  datasource:
    url:jdbc:postgresql://localhost:5432/testDB
    username:yuvraj
    password:12345678

  jpa:
    hibernate:
      ddl-auto:update
    show-sql:true
    properties:
      hibernate;
        dialect: org.hibernate.dialect.PostgresSQLDialect
        format_sql:true
